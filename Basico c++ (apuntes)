/******************************************************************************

Welcome to GDB Online.
GDB online is an online compiler and debugger tool for C, C++, Python, Java, PHP, Ruby, Perl,
C#, OCaml, VB, Swift, Pascal, Fortran, Haskell, Objective-C, Assembly, HTML, CSS, JS, SQLite, Prolog.
Code, Compile, Run and Debug online from anywhere in world.

*******************************************************************************/
#include <iostream>

using namespace std;

int main()
{
    cout<<"Esta es la configuracion basica de todo programa de c++. ";
    
    cout<<"librerias.- son el conjunto de funciones y objetos que se puede usar en los programas y asi no escribir codigos desde 0. ";
    
    cout<<"using namespace std.- sirve para no escribir std en cada funcion como se haria en c. ";
    
    cout<<"int main.- es la funcion fundamental que marca donde comienza el programa. ";
    
    cout<<"{} estos se escriben para mostrar donde empieza y termina el programa. ";
   
    cout<<"return 0.- es la funcion que indica que el programa funciono con exito, si la funcion devuelve un valor distinto a 0 es porque el programa contiene errores. ";

    return 0;
}
